[project]
name = "pyclustifyr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Michael Kaufman", email = "MLKaufman@users.noreply.github.com" }
]
requires-python = ">=3.11"
dependencies = [
    "anndata>=0.12.19",
    "matplotlib>=3.11.0",
    "numpy>=2.4.6",
    "pandas>=2.3.3",
    "requests>=2.34.2",
    "scipy>=1.17.1",
]

[build-system]
requires = ["uv_build>=0.11.26,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.21",
]
